package captureReplay;

/**
 * ENUM for the different input events used by InputReader and InputRecorder
 */
public enum FakeInput {
    KEY_DOWN,
    KEY_UP,
    KEY_TYPED,
    WAIT,
    TOUCH_UP,
    TOUCH_DOWN
}
